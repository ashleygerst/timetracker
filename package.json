{
  "name": "time-tracker",
  "version": "1.0.0",
  "description": "East West Manufacturing Time Tracker",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server"
  },
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.9.1"
  }
}
